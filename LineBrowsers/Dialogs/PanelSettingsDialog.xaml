<Window x:Class="LineBrowsers.Dialogs.PanelSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Title.PanelSettings}" SizeToContent="Height" Width="480"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource WindowBackgroundBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="{DynamicResource Label.Url}"
               Foreground="{DynamicResource TextForegroundBrush}" VerticalAlignment="Center"/>
        <TextBox x:Name="UrlBox" Grid.Row="0" Grid.Column="1"
                 Margin="0,4" VerticalContentAlignment="Center"/>
        <Button Grid.Row="0" Grid.Column="2" Content="{DynamicResource Btn.GetCurrentUrl}"
                Margin="8,4,0,4" Padding="8,2" Click="GetCurrentUrl_Click"/>

        <Label Grid.Row="1" Grid.Column="0" Content="{DynamicResource Label.Width}"
               Foreground="{DynamicResource TextForegroundBrush}" VerticalAlignment="Center"/>
        <TextBox x:Name="WidthBox" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2"
                 Margin="0,4" VerticalContentAlignment="Center"/>

        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3"
                    Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="{DynamicResource Btn.OK}" Width="72" Margin="0,0,8,0"
                    IsDefault="True" Click="OK_Click"/>
            <Button Content="{DynamicResource Btn.Cancel}" Width="72" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
